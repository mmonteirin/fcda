/**
 * MapComponents.web.js
 * Stub para Web - exporta valores null para evitar erros
 */

export const MapView = null;
export const Marker = null;
export const Circle = null;
export const PROVIDER_GOOGLE = null;

export const getMapComponents = () => {
  return {
    MapView: null,
    Marker: null,
    Circle: null,
    PROVIDER_GOOGLE: null,
  };
};
